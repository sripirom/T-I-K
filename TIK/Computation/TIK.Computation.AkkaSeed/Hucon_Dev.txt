
akka {
  loglevel = OFF

  actor {
    provider = cluster
    debug {
      receive = on
      autoreceive = on
      lifecycle = on
      event-stream = on
      unhandled = on
    }
  }

  remote {
    dot-netty.tcp {
      hostname = localhost      # external (logical) hostname
      port = 0                   # external (logical) port

      # bind-hostname = localhost # internal (bind) hostname
      # bind-port = 5301              # internal (bind) port
    }
  }

  cluster {
    seed-nodes = ["akka.tcp://OnlineSystem@localhost:5301"]
    rules = ["Membership", "commonStrockRoute", "commonStrocks", "eodStocks"]
  }

}      


